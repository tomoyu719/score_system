name: score_core
description: Core data model, CommandEngine, and Validation for score_system.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  fast_immutable_collections: ^11.2.0
  uuid: ^4.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
