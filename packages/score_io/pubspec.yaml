name: score_io
description: File format I/O for score_system (MusicXML, MIDI, .score.json).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  score_core:
    path: ../score_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
