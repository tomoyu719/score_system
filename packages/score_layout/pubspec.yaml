name: score_layout
description: Layout calculation and collision detection for score_system.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  fast_immutable_collections: ^11.2.0
  score_core:
    path: ../score_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
